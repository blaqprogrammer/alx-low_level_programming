#include "main.h"

/**
 *print_alphabet_x10- runs the code
 *
 * Return: alphabets from a - z.
 */

int _islower(int c)
{
	for (c = 'a'; c <= 'z';)
		if (c == c)
	{
		_putchar(1);
	}
	_putchar('\n');
}
