#include "main.h"
#include <string.h>

/**
 *_strlen - The entry point
 *@s: The main int
 *Return: Returns the lenght of a string.
 */

int _strlen(char *s)
{
	int lenght;

	lenght = _strlen(s);
	_putchar("%c", lenght);
	return (0);
}
