#include "main.h"
#include <string.h>

/**
 *_strlen - The entry point
 *@s: The main int
 *Return: Returns the lenght of a string.
 */

int _strlen(char *s, size_t)
{
	s = " ";
	size_t lenght;

	lenght = _strlen(s);
	return (lenght);
}
