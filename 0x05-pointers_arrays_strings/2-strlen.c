#include "main.h"
#include <string.h>

/**
 *_strlen - The entry point
 *@s: The main int
 *Return: Returns the lenght of a string.
 */

size_t strlen(char const *s)
{
	s[] = " ";
	size_t lenght;

	lenght = _strlen(s);
	return (lenght);
}
