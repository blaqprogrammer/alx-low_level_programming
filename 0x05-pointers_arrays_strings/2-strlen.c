#include "main.h"

/**
 *_strlen - The entry point
 *@s: The main int
 *Return: Returns the lenght of a string.
 */

int _strlen(char *s)
{
	size_t len = 0;

	while (*str++)
		len++;
	return (len);
}
