#include "main.h"

/**
 * _isdigit - This is the entry point.
 * @c: Holds the data
 * Return: Returns 1 or 0
 */




int _isdigit(int c)
{
	if (c <= 9)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
