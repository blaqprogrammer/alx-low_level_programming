#include "main.h"

/**
 * _isupper - this is the entrt point.
 *@c: integer that carries the command
 * Return: Returns 1 if caps and 0 if not caps.
 */




int _isupper(int c)
{
	if (c == 'C')
	{
		_putchar('1');
	}
	else
	{
		return (0);
	}
}
