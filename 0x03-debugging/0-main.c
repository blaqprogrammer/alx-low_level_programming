#include "main.h"



/**
 *positive_or_negative:-This is the entry point.
 *@i: The is the integer that stores the number
 * Return: 0 if positive or negative
 */



int positive_or_negative(int i)
{
	i = 0;
	positive_or_negative(i);
	return (0);
}
